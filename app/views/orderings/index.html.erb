<div data-turbolinks= "false" >
<h1>Categories</h1>
<ul>
    <li><%= link_to 'All', "/orderings", class: "categories-menu" %></li>
    <% @categories.each do |category| %>
    <li><%= link_to category.name, "/orderings?category_id=#{category.id}", class:"categories-menu" %></li>
    <% end %>
</ul>
</div>
<h1>Menu Items</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Unit price</th>
      <th>Category</th>
      <th>Quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<div data-turbolinks= "false" >
  <tbody id="menu_items">
    <%# @menu_items.each do |menu_item| %>
        <%#= form_tag '/orderings/order' do %>
          <!--<tr>
            <%#= hidden_field_tag 'menu_item_id', menu_item.id %>
            <%# if menu_item.image.attached? %>
              <td><%#= image_tag menu_item.image.variant(resize: "100x100") %></td>
            <%# else %>
              <td>No available</td>
            <%# end %>
            <td><%#= menu_item.name %></td>
            <td><%#= menu_item.unit_price %></td>
            <td><%#= menu_item.category.name %></td>
            <td><%#= number_field_tag 'quantity' %></td>
            <td><%#= link_to 'Show', menu_item %></td>
            <%# if is_admin? %>
              <td><%#= link_to 'Edit', edit_menu_item_path(menu_item) %></td>
              <td><%#= link_to 'Destroy', menu_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <%# end %>
            <td><%#= submit_tag 'Add to cart'%></td>
          </tr>
        <%# end %>
    <%# end %>-->
  </tbody>
</div>
</table>
<br>
<% if is_admin? %>
<%= link_to 'New Menu Item', new_menu_item_path %>
<% end %>
<hr></hr>

<h1>Ordered Items</h2>


<table>
  <% if @order.present? %>
  <tr>
    <th>Name</th>
    <th>Quantity</th>
    <th>Price</th>
  </tr>
  
    <% @order.ordered_items.each do |ordered_item| %>
    <tr>
      <td><%= ordered_item.menu_item.name %></td>
      <td><%= ordered_item.quantity %></td>
      <td><%= ordered_item.menu_item.unit_price %></td>
    </tr>
    <% end %>
  <% else %>
  <p>Please order something ya</p>
  <% end %>
</table>


<% if @order.present? %>
<%= link_to "Check Out", checkout_orderings_path %>
<% end %>